export type CrexiProperty = {
    // @TODO missing implementation
}

export type MontoProperty = {
    id: string;
    name: string;
    price: number;
    types: string[];
    location: string;
    status: string;
    imageUrl: string;
}

